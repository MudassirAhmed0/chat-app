subscription OnMessageAdded($conversationId: ID!) {
  messageAdded(conversationId: $conversationId) {
    id
    conversationId
    content
    type
    createdAt
    sender {
      id
      username
    }
    clientRequestId
    reactions {
      emoji
      userId
      user {
        username
      }
    }
  }
}
